/**
 * Multiavatar - SVG avatar generation library
 * 
 * This is a simplified version. For the full library, visit:
 * https://github.com/multiavatar/multiavatar
 * 
 * @version 1.0.0
 */

(function(root, factory) {
    if (typeof define === 'function' && define.amd) {
        define([], factory);
    } else if (typeof module === 'object' && module.exports) {
        module.exports = factory();
    } else {
        root.multiavatar = factory();
    }
}(typeof self !== 'undefined' ? self : this, function() {
    'use strict';

    /**
     * Generate a deterministic hash from text
     */
    function hashCode(str) {
        let hash = 0;
        for (let i = 0; i < str.length; i++) {
            const char = str.charCodeAt(i);
            hash = ((hash << 5) - hash) + char;
            hash = hash & hash;
        }
        return Math.abs(hash);
    }

    /**
     * Generate color from hash
     */
    function generateColor(hash, index) {
        const h = (hash + index * 137) % 360;
        const s = 60 + (hash % 20);
        const l = 50 + ((hash + index) % 15);
        return `hsl(${h}, ${s}%, ${l}%)`;
    }

    /**
     * Generate SVG avatar
     */
    function multiavatar(text, size) {
        if (!text) {
            text = 'default';
        }
        
        size = size || 200;
        
        const hash = hashCode(text);
        const color1 = generateColor(hash, 0);
        const color2 = generateColor(hash, 1);
        const color3 = generateColor(hash, 2);
        
        // Generate different avatar styles based on hash
        const style = hash % 5;
        
        let svg = '';
        
        switch(style) {
            case 0:
                svg = generateStyle0(color1, color2, color3);
                break;
            case 1:
                svg = generateStyle1(color1, color2, color3);
                break;
            case 2:
                svg = generateStyle2(color1, color2, color3);
                break;
            case 3:
                svg = generateStyle3(color1, color2, color3);
                break;
            case 4:
                svg = generateStyle4(color1, color2, color3);
                break;
        }
        
        return svg;
    }

    function generateStyle0(c1, c2, c3) {
        return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
            <rect width="200" height="200" fill="${c1}"/>
            <circle cx="100" cy="80" r="50" fill="${c2}"/>
            <circle cx="80" cy="70" r="10" fill="${c3}"/>
            <circle cx="120" cy="70" r="10" fill="${c3}"/>
            <ellipse cx="100" cy="100" rx="20" ry="15" fill="${c3}"/>
        </svg>`;
    }

    function generateStyle1(c1, c2, c3) {
        return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
            <rect width="200" height="200" fill="${c1}"/>
            <polygon points="100,30 170,170 30,170" fill="${c2}"/>
            <circle cx="80" cy="110" r="8" fill="${c3}"/>
            <circle cx="120" cy="110" r="8" fill="${c3}"/>
            <rect x="80" y="130" width="40" height="10" rx="5" fill="${c3}"/>
        </svg>`;
    }

    function generateStyle2(c1, c2, c3) {
        return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
            <rect width="200" height="200" fill="${c1}"/>
            <rect x="40" y="40" width="120" height="120" rx="20" fill="${c2}"/>
            <circle cx="75" cy="85" r="12" fill="${c3}"/>
            <circle cx="125" cy="85" r="12" fill="${c3}"/>
            <path d="M 75 120 Q 100 140 125 120" stroke="${c3}" stroke-width="6" fill="none"/>
        </svg>`;
    }

    function generateStyle3(c1, c2, c3) {
        return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
            <rect width="200" height="200" fill="${c1}"/>
            <circle cx="100" cy="100" r="70" fill="${c2}"/>
            <circle cx="75" cy="85" r="15" fill="${c3}"/>
            <circle cx="125" cy="85" r="15" fill="${c3}"/>
            <circle cx="100" cy="120" r="20" fill="${c3}" opacity="0.5"/>
        </svg>`;
    }

    function generateStyle4(c1, c2, c3) {
        return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
            <rect width="200" height="200" fill="${c1}"/>
            <path d="M 100 30 L 170 100 L 100 170 L 30 100 Z" fill="${c2}"/>
            <circle cx="85" cy="90" r="10" fill="${c3}"/>
            <circle cx="115" cy="90" r="10" fill="${c3}"/>
            <line x1="85" y1="115" x2="115" y2="115" stroke="${c3}" stroke-width="6" stroke-linecap="round"/>
        </svg>`;
    }

    return multiavatar;
}));
